package gfs;

import utils.radixtree.Node;
import utils.radixtree.RadixTree;

import java.util.*;

public class NameSpaceManager {
    private FileMetadata root;

    public NameSpaceManager(String rootKey, FileMetadata value) {
    }
}
